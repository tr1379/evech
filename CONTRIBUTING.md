# 贡献指南

感谢您对 Evech 项目的兴趣！我们欢迎各种形式的贡献，包括代码、文档、设计和反馈。本文档将指导您如何有效地为项目做出贡献。

---

## 📋 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [报告 Bug](#报告-bug)
- [建议功能](#建议功能)
- [提交代码](#提交代码)
- [代码风格](#代码风格)
- [提交信息规范](#提交信息规范)
- [Pull Request 流程](#pull-request-流程)
- [开发环境设置](#开发环境设置)
- [常见问题](#常见问题)

---

## 行为准则

### 我们的承诺

为了营造一个开放和欢迎的环境，我们作为贡献者和维护者承诺：

- 对所有人保持尊重和包容
- 欢迎不同的观点和经验
- 接受建设性的批评
- 关注对项目最有利的事情
- 对其他社区成员表现出同情

### 不可接受的行为

以下行为在我们的社区中是不可接受的：

- 使用带有性别、性取向、种族、宗教或其他特征的语言或图像
- 人身攻击或骚扰
- 发布他人的私人信息（如物理地址或电子邮件）
- 其他可能被合理认为在专业环境中不适当的行为

---

## 如何贡献

### 贡献的方式

1. **报告 Bug** - 帮助我们发现和修复问题
2. **建议功能** - 提出改进和新功能的想法
3. **改进文档** - 增强项目文档的清晰度和完整性
4. **提交代码** - 实现新功能或修复现有问题
5. **代码审查** - 审查他人的 Pull Request
6. **分享反馈** - 提供关于用户体验的见解

---

## 报告 Bug

### 提交 Bug 报告前

在创建 Bug 报告前，请先检查 [issue list](https://github.com/tr1379/evech/issues)，确保问题尚未被报告。

### 如何提交 Bug 报告

使用以下模板提交 Bug 报告：

**标题**: 简明扼要地描述问题

**描述**:
```
[问题的简要描述]

## 复现步骤

1. [第一步]
2. [第二步]
3. [...]

## 预期行为

[应该发生什么]

## 实际行为

[实际发生了什么]

## 环境信息

- 操作系统: [例如 macOS 12.1, Windows 11, Ubuntu 22.04]
- 浏览器: [例如 Chrome 98, Safari 15, Firefox 97]
- Node.js 版本: [例如 18.0.0]
- Evech 版本: [例如 1.0.0]

## 附加信息

[任何其他相关信息，如错误日志、截图等]
```

### Bug 报告的最佳实践

- 使用清晰的、描述性的标题
- 提供具体的复现步骤
- 提供具体的示例来演示这些步骤
- 描述您观察到的行为以及您期望的行为
- 包含屏幕截图或动画 GIF（如果可能）
- 包含您的环境信息

---

## 建议功能

### 提交功能建议前

在创建功能建议前，请先检查 [issue list](https://github.com/tr1379/evech/issues)，确保功能尚未被建议。

### 如何提交功能建议

使用以下模板提交功能建议：

**标题**: 简明扼要地描述建议的功能

**描述**:
```
[功能的简要描述]

## 问题描述

[描述您想解决的问题。例如："当我尝试 [...] 时，我总是 [...]"]

## 建议的解决方案

[清晰地描述您希望发生的事情]

## 替代方案

[描述您考虑过的任何替代解决方案或功能]

## 附加信息

[任何其他上下文或屏幕截图]
```

### 功能建议的最佳实践

- 使用清晰的、描述性的标题
- 提供详细的功能描述
- 解释为什么这个功能会有用
- 列出一些其他项目中的相似功能示例
- 考虑实现的复杂性

---

## 提交代码

### 开发工作流

1. **Fork 仓库**
   ```bash
   # 访问 https://github.com/tr1379/evech
   # 点击 "Fork" 按钮
   ```

2. **克隆您的 Fork**
   ```bash
   git clone https://github.com/YOUR_USERNAME/evech.git
   cd evech
   ```

3. **添加上游仓库**
   ```bash
   git remote add upstream https://github.com/tr1379/evech.git
   ```

4. **创建特性分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

5. **进行更改**
   - 编写代码
   - 添加测试
   - 更新文档

6. **提交更改**
   ```bash
   git add .
   git commit -m "feat: add your feature description"
   ```

7. **推送到您的 Fork**
   ```bash
   git push origin feature/your-feature-name
   ```

8. **创建 Pull Request**
   - 访问原始仓库
   - 点击 "New Pull Request"
   - 选择您的分支
   - 填写 PR 模板

---

## 代码风格

### TypeScript/JavaScript

遵循以下代码风格指南：

```typescript
// ✅ 好的做法

// 使用 const，避免 var
const name = "Evech";

// 使用有意义的变量名
const getUserById = async (id: number) => {
  return await db.query.users.findFirst({
    where: (users, { eq }) => eq(users.id, id),
  });
};

// 使用类型注解
interface Post {
  id: number;
  title: string;
  content: string;
}

// 使用箭头函数
const handleClick = () => {
  console.log("Clicked!");
};

// ❌ 避免

// 避免 var
var name = "Evech";

// 避免单字母变量
const x = getUserById(1);

// 避免 any 类型
const post: any = getPost();

// 避免过长的函数
const doEverything = () => {
  // 200 行代码...
};
```

### React 组件

```typescript
// ✅ 好的做法

import { FC } from "react";
import { Button } from "@/components/ui/button";

interface PostCardProps {
  title: string;
  excerpt: string;
  onRead: () => void;
}

export const PostCard: FC<PostCardProps> = ({
  title,
  excerpt,
  onRead,
}) => {
  return (
    <div className="p-4 border rounded-lg">
      <h3 className="text-lg font-semibold">{title}</h3>
      <p className="text-muted-foreground">{excerpt}</p>
      <Button onClick={onRead}>阅读更多</Button>
    </div>
  );
};

// ❌ 避免

// 避免大型组件
const HugeComponent = () => {
  // 500 行代码...
};

// 避免过多的状态
const [state1, setState1] = useState();
const [state2, setState2] = useState();
const [state3, setState3] = useState();
// ...
```

### 样式

- 使用 Tailwind CSS 类
- 避免内联样式
- 使用 CSS 变量进行主题颜色
- 保持类名简洁

```typescript
// ✅ 好的做法
<div className="p-4 bg-background text-foreground rounded-lg">
  Content
</div>

// ❌ 避免
<div style={{ padding: "16px", backgroundColor: "#fff" }}>
  Content
</div>
```

### 格式化

使用 Prettier 自动格式化代码：

```bash
# 格式化所有文件
pnpm format

# 格式化特定文件
pnpm prettier --write src/pages/Home.tsx
```

---

## 提交信息规范

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型 (Type)

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码风格更改（不影响功能）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建、依赖等维护任务

### 示例

```bash
# 新功能
git commit -m "feat(comments): add comment like feature"

# Bug 修复
git commit -m "fix(auth): resolve OAuth callback issue"

# 文档更新
git commit -m "docs(readme): update installation instructions"

# 性能优化
git commit -m "perf(posts): optimize database queries"
```

---

## Pull Request 流程

### PR 模板

```markdown
## 描述

[简要描述您的更改]

## 关联的 Issue

Closes #[issue number]

## 更改类型

- [ ] Bug 修复
- [ ] 新功能
- [ ] 文档更新
- [ ] 性能优化
- [ ] 代码重构

## 更改清单

- [ ] 我已阅读 CONTRIBUTING 指南
- [ ] 我的代码遵循项目的代码风格
- [ ] 我已添加测试来覆盖我的更改
- [ ] 所有新的和现有的测试都通过了
- [ ] 我已更新相关文档

## 测试说明

[描述您如何测试这些更改]

## 屏幕截图（如果适用）

[添加相关的屏幕截图或 GIF]
```

### PR 审查流程

1. **自动检查**
   - 代码风格检查
   - 单元测试
   - 构建验证

2. **代码审查**
   - 至少一名维护者审查
   - 讨论和改进建议
   - 批准或请求更改

3. **合并**
   - 所有检查通过
   - 至少一个批准
   - 分支自动删除

### PR 最佳实践

- 保持 PR 规模合理（不超过 400 行代码）
- 一个 PR 只解决一个问题或实现一个功能
- 提供清晰的描述和上下文
- 及时回应审查反馈
- 保持分支最新（定期 rebase）

---

## 开发环境设置

### 系统要求

- Node.js 22.13.0+
- pnpm 10.4.1+
- Git
- MySQL 8.0+ 或 TiDB

### 安装步骤

1. **克隆仓库**
   ```bash
   git clone https://github.com/tr1379/evech.git
   cd evech
   ```

2. **安装依赖**
   ```bash
   pnpm install
   ```

3. **配置环境变量**
   ```bash
   cp .env.example .env
   # 编辑 .env 文件
   ```

4. **初始化数据库**
   ```bash
   pnpm db:push
   pnpm seed
   ```

5. **启动开发服务器**
   ```bash
   pnpm dev
   ```

### 常用命令

```bash
# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start

# 运行测试
pnpm test

# 运行测试（监视模式）
pnpm test --watch

# 格式化代码
pnpm format

# 类型检查
pnpm check

# 推送数据库架构
pnpm db:push

# 生成数据库迁移
pnpm db:generate
```

### IDE 推荐

- **VS Code** - 推荐配置
  - 安装 ESLint 扩展
  - 安装 Prettier 扩展
  - 安装 Tailwind CSS IntelliSense 扩展
  - 安装 TypeScript Vue Plugin 扩展

### 调试

**VS Code 调试配置** (`.vscode/launch.json`):

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Server",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["dev"],
      "console": "integratedTerminal"
    }
  ]
}
```

---

## 常见问题

### Q: 如何开始贡献？

A: 从查看 [open issues](https://github.com/tr1379/evech/issues) 开始。查找标记为 `good first issue` 或 `help wanted` 的问题。

### Q: 我可以贡献什么？

A: 任何东西！代码、文档、设计、测试、翻译等。查看 [如何贡献](#如何贡献) 部分了解更多。

### Q: 我的 PR 被拒绝了，怎么办？

A: 这很正常！阅读反馈，进行必要的更改，然后重新提交。我们的目标是帮助您改进代码。

### Q: 需要多长时间才能审查我的 PR？

A: 通常在 1-3 天内。复杂的 PR 可能需要更长时间。

### Q: 我可以在 main 分支上工作吗？

A: 不可以。请始终创建特性分支。

### Q: 如何更新我的 Fork？

A: 
```bash
git fetch upstream
git rebase upstream/main
git push origin main
```

### Q: 如何处理合并冲突？

A:
```bash
# 更新到最新的 main
git fetch upstream
git rebase upstream/main

# 解决冲突
# 编辑有冲突的文件
git add .
git rebase --continue

# 强制推送到您的 Fork
git push origin feature/your-feature -f
```

### Q: 我需要签署 CLA 吗？

A: 不需要。我们不需要 Contributor License Agreement。

### Q: 我可以贡献多个 PR 吗？

A: 当然可以！我们欢迎多个贡献。

---

## 获取帮助

- **问题和讨论**: [GitHub Issues](https://github.com/tr1379/evech/issues)
- **实时讨论**: [GitHub Discussions](https://github.com/tr1379/evech/discussions)
- **邮件**: support@evech.dev

---

## 致谢

感谢所有为 Evech 做出贡献的人！您的努力帮助我们打造更好的博客平台。

---

**最后更新**: 2026-02-10  
**版本**: 1.0.0
